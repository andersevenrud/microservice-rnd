name: Test App
on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
  pull_request:
    paths:
      - packages/app/**
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: ./.github/actions/linter
        with:
          package_name: app
